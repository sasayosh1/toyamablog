# ユーザー名一括置換プロンプト

このファイルは、テンプレート内のプレースホルダー `{{USER_NAME}}` を、あなたの名前に一括変換するためのAI用指示書です。

---

## AIへの指示プロンプト

あなたは優秀なファイル編集アシスタントです。
現在のディレクトリにある以下のファイルに含まれる `{{USER_NAME}}` という文字列を、私の名前にすべて置換してください。

### 1. ユーザー名の一括置換 ({{USER_NAME}})

#### 対象ファイル一覧
1.  `00_システム/00_UserProfile/00_マスター(Master_Context).md`
2.  `00_システム/00_UserProfile/01_価値観(Core_Values).md`
3.  `00_システム/00_UserProfile/03_執筆スタイル(Style_Guidelines).md`
4.  `.agent/rules/2nd-brain-rule.md`
5.  `00_システム/00_UserProfile/06_ルーティン(Routine_Rules).md`
6.  `.agent/workflows/weekly-review.md`
7.  `.agent/workflows/x-long.md`
8.  `.agent/workflows/deep-learn.md`
9.  `.agent/workflows/sabun-learn.md`
10. `00_システム/01_Prompts/X長文ポスト作成プロンプト/*.md` (フォルダ内の全マークダウンファイル)

#### 実行手順
1. **ユーザーへの質問**:
   「あなたの名前を教えてください。（入力例：リツト）」と質問し、入力を待ちます。
2. **置換実行**:
   上記ファイルの `{{USER_NAME}}` (または `リツト`, `Ritsuto`) を、ユーザーの名前に一括置換してください。
   ※ `start_ears.bat` などのスクリプトタイトルも書き換えてください。
   
   > ⚠️ **除外対象**: `03_知識ベース/人の情報（relationship）/リツト(Ritsuto).md` は**変更しないでください**。このファイルは教材制作者のプロフィールであり、ユーザーの名前に置換する対象ではありません。

---

### 2. ルートパスの一括修正 (Path Correction)

テンプレート内のプロンプトには、制作者環境の絶対パス `c:\Users\PC_User\product\Ritsuto_Brain` が残っています。
これを、**現在のあなたの環境のルートパス** に書き換える必要があります。

#### 対象ファイル
- `00_システム/01_Prompts` 以下のほぼ全てのファイル
- `.agent/workflows` 以下のワークフローファイル

#### 実行手順
1. **現在パスの特定**:
   システムコマンド (`pwd` や `Get-Location`) を使用して、現在このファイルがあるルートディレクトリ (`2nd-Brain` フォルダのフルパス) を取得してください。
   例: `C:\Users\You\Documents\2nd-Brain`
2. **置換実行**:
   `grep` 等を使って、ファイル内に残っている `c:\Users\PC_User\product\Ritsuto_Brain` という文字列を検索し、**ステップ1で取得した「あなたの環境のパス」** に全て一括置換してください。

---

実行をお願いします。

### 3. 音声認識環境のセットアップ (Auto Setup)

置換作業が完了したら、引き続き音声認識環境の自動セットアップを行います。

1.  **OS自動判定と実行**:
    現在の環境が Windows か Mac/Linux かを判定し、適切な以下のセットアップコマンドを実行してください。
    - **Windowsの場合**: `00_システム/devtools/setup_ears.bat`
    - **Mac/Linuxの場合**: `bash 00_システム/devtools/setup_ears.sh`
    ※このスクリプトは、音声認識に必要なPython環境やライブラリを自動インストールします。

---

### 4. 次のステップへ (Next Step)

名前更新と初期セットアップが完了しました。

> **Next Step:**
> 続いて、**`02_コンテキスト注入.md`** を開き、その指示に従って「コンテキスト（文脈）の注入」を実行してください。
>
> ※このファイル (`01`) は、全てのセットアップが完了するまで削除せずに残しておくことを推奨します（後でまとめて削除します）。
